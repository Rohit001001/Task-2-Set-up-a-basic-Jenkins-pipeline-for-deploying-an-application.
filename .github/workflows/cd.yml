name: CD - Deploy Docker Image to AWS EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 🔑 SSH into EC2 and deploy
      - name: SSH into EC2 and run container
        uses: appleboy/ssh-action@v1.0.0
        with:
